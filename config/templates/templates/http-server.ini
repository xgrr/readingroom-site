[uwsgi]
chdir = {{paths.base}}
{%- if django_sock %}
socket =  {{django_sock}}
{%- else %}
http-socket = {{django_server}}:{{django_serve_port}}
{%- endif %}
module = {{django_app}}.wsgi:application
venv = {{paths.env}}
master = true
processes = 1
